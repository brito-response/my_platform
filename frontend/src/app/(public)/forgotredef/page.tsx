import { FormRedef } from "@/forms";

export default function ForgotREfef() {
    return (
        <div className="min-h-screen flex items-center justify-center bg-(--bg-section-100)">
            <FormRedef />
        </div>
    );
}